function scrollTo(element) {
    window.scroll({
        left: 0,
        top: element.offsetTop - 100,
        behavior: 'smooth'
    })
}

// все функции хедера
headerButtonProfessions.addEventListener('click', () => {
    scrollTo(professionsBlock)
})

headerButtonLife.addEventListener('click', () => {
    scrollTo(lifeBlock)
})

headerButtonTeachers.addEventListener('click', () => {
    scrollTo(teachersBlock)
})

headerButtonFeedback.addEventListener('click', () => {
    scrollTo(feedbackBlock)
})

headerButtonForms.addEventListener('click', () => {
    scrollTo(formsBlock)
})

headerButtonCourses.addEventListener('click', () => {
    scrollTo(coursesBlock)
})

headerButtonDocs.addEventListener('click', () => {
    scrollTo(docsBlock)
})

introButtonDocs.addEventListener('click', () => {
    scrollTo(docsBlock)
})

headerButtonMap.addEventListener('click', () => {
    scrollTo(mapBlock)
})

headerButtonContacts.addEventListener('click', () => {
    scrollTo(contactsBlock)
})

// все функции футера
footerButtonProfessions.addEventListener('click', () => {
    scrollTo(professionsBlock)
})

footerButtonLife.addEventListener('click', () => {
    scrollTo(lifeBlock)
})

footerButtonTeachers.addEventListener('click', () => {
    scrollTo(teachersBlock)
})

footerButtonFeedback.addEventListener('click', () => {
    scrollTo(feedbackBlock)
})

footerButtonForms.addEventListener('click', () => {
    scrollTo(formsBlock)
})

footerButtonCourses.addEventListener('click', () => {
    scrollTo(coursesBlock)
})

footerButtonDocs.addEventListener('click', () => {
    scrollTo(docsBlock)
})

footerButtonMap.addEventListener('click', () => {
    scrollTo(mapBlock)
})

footerButtonContacts.addEventListener('click', () => {
    scrollTo(contactsBlock)
})

let element = addEventListener('click', () => {

})